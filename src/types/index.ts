export interface RoomType {
  id: number
  images: string[]
  title: string
  address: string
  price: number
  category: string
}

export interface FaqType {
  id: number
  title: string
  desc: string
}
